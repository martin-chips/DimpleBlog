version : '3.8'
services:
  dimple-nacos:
    container_name: dimple-nacos
    image: nacos/nacos-server
    build:
      context: ./nacos
    environment:
      - MODE=standalone
    volumes:
      - /home/docker/nacos/logs/:/home/nacos/logs
      - /home/docker/nacos/conf/application.properties:/home/nacos/conf/application.properties
    ports:
      - "8848:8848"
      - "9848:9848"
      - "9849:9849"
    depends_on:
      - dimple-mysql
  dimple-mysql:
    container_name: dimple-mysql
    image: mysql:5.7
    build:
      context: ./mysql
    ports:
      - "3306:3306"
    volumes:
      - /home/docker/mysql/conf:/etc/mysql/conf.d
      - /home/docker/mysql/logs:/logs
      - /home/docker/mysql/data:/var/lib/mysql
    command: [
          'mysqld',
          '--innodb-buffer-pool-size=80M',
          '--character-set-server=utf8mb4',
          '--collation-server=utf8mb4_unicode_ci',
          '--default-time-zone=+8:00',
          '--lower-case-table-names=1'
        ]
    environment:
      MYSQL_DATABASE: 'dimple-cloud'
      MYSQL_ROOT_PASSWORD: password
  dimple-redis:
    container_name: dimple-redis
    image: redis
    build:
      context: ./redis
    ports:
      - "6379:6379"
    volumes:
      - /home/docker/redis/conf/redis.conf:/home/ruoyi/redis/redis.conf
      - /home/docker/redis/data:/data
    command: redis-server /home/docker/redis/config/redis.conf
  dimple-nginx:
    container_name: dimple-nginx
    image: nginx
    build:
      context: ./nginx
    ports:
      - "80:80"
    volumes:
      - /home/docker/nginx/html/dist:/home/ruoyi/projects/dimple-ui
      - /home/docker/nginx/conf/nginx.conf:/etc/nginx/nginx.conf
      - /home/docker/nginx/logs:/var/log/nginx
      - /home/docker/nginx/conf.d:/etc/nginx/conf.d
    depends_on:
      - dimple-gateway
    links:
      - dimple-gateway
  dimple-gateway:
    container_name: dimple-gateway
    build:
      context: dimple/gateway
      dockerfile: dockerfile
    ports:
      - "8080:8080"
    depends_on:
      - dimple-redis
    links:
      - dimple-redis
  dimple-auth:
    container_name: dimple-auth
    build:
      context: dimple/auth
      dockerfile: dockerfile
    ports:
      - "9200:9200"
    depends_on:
      - dimple-redis
    links:
      - dimple-redis
  dimple-modules-system:
    container_name: dimple-modules-system
    build:
      context: dimple/modules/system
      dockerfile: dockerfile
    ports:
      - "9201:9201"
    depends_on:
      - dimple-redis
      - dimple-mysql
    links:
      - dimple-redis
      - dimple-mysql
  dimple-modules-gen:
    container_name: dimple-modules-gen
    build:
      context: dimple/modules/gen
      dockerfile: dockerfile
    ports:
      - "9202:9202"
    depends_on:
      - dimple-mysql
    links:
      - dimple-mysql
  dimple-modules-job:
    container_name: dimple-modules-job
    build:
      context: dimple/modules/job
      dockerfile: dockerfile
    ports:
      - "9203:9203"
    depends_on:
      - dimple-mysql
    links:
      - dimple-mysql
  dimple-modules-file:
    container_name: dimple-modules-file
    build:
      context: dimple/modules/file
      dockerfile: dockerfile
    ports:
      - "9300:9300"
    volumes:
    - /home/docker/ruoyi/uploadPath:/home/ruoyi/uploadPath
  dimple-visual-monitor:
    container_name: dimple-visual-monitor
    build:
      context: dimple/visual/monitor
      dockerfile: dockerfile
    ports:
      - "9100:9100"
